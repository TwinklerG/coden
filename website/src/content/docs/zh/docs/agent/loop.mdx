---
title: Agent 循环
description: 理解 turn、model step、工具调用与结果如何组成循环。
sidebar:
  order: 210
---

Coding Agent 不是“一次提示词生成一次答案”。一个用户 turn 可以包含多个 model step：每一步都把当前消息和工具定义发送给 Provider；只要 assistant 返回 tool calls，CodeN 就执行并追加 tool messages，然后再次请求模型。只有不再调用工具且 Stop Hook 未要求继续时，turn 才完成。

## 循环结构

```text
user input
  → hook: UserPromptSubmit (can block)
  → model request (tool definitions + context)
  → if tool_calls: execute each tool → fill results back
  → request the model again (same turn)
  → until the model stops calling tools → hook: Stop (can demand more work)
  → return the final answer
```

每个模型请求与工具执行序列构成一个 turn；默认 `maxSteps` 为 80，达到上限后抛 `runtime.step_limit`。

## 单轮内的关键行为

| 行为 | 细节 |
| --- | --- |
| 工具执行 | 按返回顺序逐个执行；未完成的工具调用在恢复时补齐为 `runtime.interrupted` 错误 |
| 工具结果 | 入上下文前截断到 50000 字符；错误结果携带 `isError` 标记回传模型 |
| 输入变更 | `PreToolUse` Hook 更新输入后重新校验 schema、路径、风险与权限 |
| 重试 | 网络/5xx/429 错误最多重试 3 次，250ms 指数退避（0.8–1.2 随机抖动），尊重 `Retry-After` |
| 上下文错误 | 413 或上下文长度类错误触发一次紧急压缩；仍超限则抛 `context.exhausted` |
| 思考等级 | 每个 turn 开始时的等级快照贯穿该 turn 的工具循环与重试 |

## 上下文化管理

- 输入预算 = `contextWindow − reservedOutputTokens − safetyMargin`（默认 128000 − 8192 − 4096）。
- 当预估输入超过预算的 80% 时，CodeN 原样保留当前交互和上一条完整交互；当前会话模型一次性读取全部更早历史（包括已有压缩摘要），生成结构化交接摘要，输出上限为 `min(2048, reservedOutputTokens)`。
- 仅当摘要非空、不含工具调用、确实缩小被替换上下文且新投影低于硬预算时，才原子提交摘要。自动压缩失败不会修改历史，同一用户 turn 内不会重复尝试。
- `/compact` 立即运行相同的模型压缩流程并明确反馈失败；紧急压缩失败抛出 `context.exhausted`；`/new` 清除摘要并开始新对话。
- 语义压缩本质上仍有损。持久的项目规则和关键状态应写入项目说明文件，不应只依赖对话历史。

## 完成与失败

- **正常完成**：模型输出不含 tool_calls；若 `Stop` Hook 返回 `decision: "block"`，会注入反馈并要求模型继续（仍受 `maxSteps` 限制）。
- **失败**：抛 `CodeNError` 并触发 `Notification` Hook（`attention_required`）与 `turn.failed` 事件；SessionEnd 结束会话。
- 每次有效 turn 的最后都会以 usage 汇总 token 用量。

## 相关参考

- [上下文与思考等级](/coden/zh/docs/agent/compaction-and-thinking/)
- [审批模式](/coden/zh/docs/safety/approval-modes/)
- [工具与风险等级](/coden/zh/docs/agent/tools-and-context/)
- [会话](/coden/zh/docs/agent/sessions/)
