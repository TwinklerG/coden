---
title: Provider 与模型
description: 配置 OpenAI、Anthropic、模型与凭据。
sidebar:
  order: 130
---

Provider 是模型协议适配层，不是 Agent 本身。CodeN 当前实现 OpenAI 与 Anthropic 两个 Provider；模型 ID 由用户选择，CodeN 不维护能力白名单，也不会在参数被服务拒绝时静默假装成功。

## 配置第一个 Provider

CodeN 只支持两个 Provider：`openai`（默认）和 `anthropic`。Provider 决定使用的 API 端点、鉴权方式与思考参数映射；默认模型是 `gpt-5-mini`。

### OpenAI（默认 Provider）

```bash
export CODEN_OPENAI_API_KEY=sk-...
coden "say hello"
```

自定义兼容端点（例如代理或兼容服务）：

```bash
export CODEN_OPENAI_API_KEY=sk-...
export CODEN_OPENAI_BASE_URL=https://api.example.com/v1
coden --model your-model-id "say hello"
```

### Anthropic

```bash
export CODEN_ANTHROPIC_API_KEY=sk-ant-...
coden --provider anthropic --model claude-sonnet-4-20250514 "say hello"
```

切换 Provider 必须显式指定 `--provider anthropic`；Provider 只接受 `openai` 或 `anthropic`，其他值在参数解析阶段报错。

### 凭据来源与优先级

CodeN 创建 Provider 时读取的是**进程环境变量**。设置方式按优先级从高到低：

1. Shell 中已导出的环境变量（`export` 后启动 CodeN）。
2. 配置文件 `env` 字段：加载配置时注入进程环境，但**不覆盖** shell 中已导出的同名变量。
3. 未设置任何 Key 时启动失败，退出码 2，错误信息形如 `CODEN_OPENAI_API_KEY is required`。

配置文件 `env` 字段示例（用户级 `~/.config/coden/config.json`）：

```json
{
  "env": {
    "CODEN_OPENAI_API_KEY": "sk-..."
  }
}
```

项目级 `<workspace>/.coden/config.json` 有同名 `env` 字段；两级合并时项目级逐键覆盖用户级。

### 密钥存放边界

- 不要把 Key 写进会提交、共享或分发的目录。`~/.config/coden/`、`.coden/`（已在 `.gitignore` 忽略）以及 config 的 `env` 字段是受支持的位置。
- CodeN 不会自动读取 `.env` 文件；`just` 命令中使用 `set dotenv-load` 仅影响 `just` 子进程的开发场景。
- 会话 JSONL 与 trace 可能包含模型推理与输入内容，不要未经审查地分享。

### 验证最小配置

```bash
coden -p "Reply with exactly: coden-ok"
```

print 模式输出纯文本并在单轮后退出，适合验证认证与端点是否正确。预期输出包含 `coden-ok`。

### 相关参考

- [配置优先级](/coden/zh/docs/operate/configuration/)
- [用户级与项目级配置](/coden/zh/docs/operate/configuration/)
- [环境变量](/coden/zh/docs/operate/configuration/)
- [OpenAI 配置](/coden/zh/docs/operate/providers/)
- [Anthropic 配置](/coden/zh/docs/operate/providers/)
- [凭据与数据安全](/coden/zh/docs/safety/security-boundaries/)

## OpenAI 配置

### 最小配置

```bash
export CODEN_OPENAI_API_KEY=sk-...
coden
```

`openai` 是默认 Provider，模型默认 `gpt-5-mini`。覆盖方式：

```bash
export CODEN_MODEL=gpt-5-mini
coden --model gpt-5-mini "your task"
```

### 兼容端点

```bash
export CODEN_OPENAI_API_KEY=sk-...
export CODEN_OPENAI_BASE_URL=https://api.example.com/v1
coden --model your-model-id "your task"
```

`CODEN_OPENAI_BASE_URL` 由 OpenAI SDK 直接使用；任何与该 SDK兼容的端点均可，但 CodeN 不维护模型能力白名单，参数按原样发送。

### 请求形态

- 使用 `chat.completions` 流式接口，`stream_options.include_usage` 获取用量。
- 工具以 OpenAI function calling 形式传递（`type: "function"`，名称/描述/JSON Schema）。
- 每次请求的 `max_completion_tokens` = `reservedOutputTokens`（默认 8192）。
- SDK 内部 `maxRetries` 为 0：重试由 Agent 循环执行（最多 3 次指数退避，见 [Agent 循环](/coden/zh/docs/agent/loop/)）。

### 思考参数

`--thinking` 等级映射到 `reasoning_effort`；`off` 映射 `minimal`（界面显示 `off→minimal`），`minimal`/`low`/`medium`/`high` 同名映射。`default` 不发送。详见 [语言与思考等级](/coden/zh/docs/agent/compaction-and-thinking/)。

### 配置示例

```json
{
  "provider": "openai",
  "model": "gpt-5-mini",
  "approvalModel": "gpt-5-mini"
}
```

### 失败模式

- 缺少 `CODEN_OPENAI_API_KEY`：启动失败（退出码 2）。
- 无效模型/端点：Provider 返回的错误会带 `provider` 类别透传；重试槽位用尽后失败。

### 相关参考

- [配置第一个 Provider](/coden/zh/docs/start/provider-and-model/)
- [环境变量](/coden/zh/docs/operate/configuration/)
- [字段参考](/coden/zh/docs/reference/configuration/)
- [常见错误](/coden/zh/docs/operate/automation-and-recovery/)

## Anthropic 配置

### 最小配置

```bash
export CODEN_ANTHROPIC_API_KEY=sk-ant-...
coden --provider anthropic --model claude-sonnet-4-20250514 "your task"
```

Anthropic 不是默认 Provider：必须显式 `--provider anthropic`（或配置 `provider: "anthropic"`）。`approvalModel` 同样使用 Anthropic 凭据。

### 配置示例

```json
{
  "provider": "anthropic",
  "model": "claude-sonnet-4-20250514",
  "approvalStrictness": "hard"
}
```

### 请求形态

- 使用 `messages.stream` 流式接口；`max_tokens` = `reservedOutputTokens`。
- 工具以 Anthropic tool 形式传递（`name`/`description`/`input_schema`）。
- SDK 内部 `maxRetries` 为 0：重试由 Agent 循环执行。
- 思考块（`thinking`/`redacted_thinking` 与签名）作为 `providerState` 持久化，在工具调用和恢复后原样回传；切换 Provider 时忽略不匹配的状态。

### 思考预算

| 等级 | 行为 |
| --- | --- |
| `default` | 不发送 thinking 参数 |
| `off` | 发送 `{ "type": "disabled" }` |
| `minimal` | 预算 1024 tokens |
| `low` / `medium` / `high` | `reservedOutputTokens` 的 25% / 50% / 75%，并截断到 `[1024, reservedOutputTokens − 1]` |
| 任意启用等级 | 要求 `reservedOutputTokens > 1024`，否则配置错误（退出码 2） |

预算计入 `reservedOutputTokens`：提高等级会减少同次输出中最终文本与工具调用可用的 token。

### 失败模式

- 缺少 `CODEN_ANTHROPIC_API_KEY`：启动失败（退出码 2）。
- `reservedOutputTokens <= 1024` 且启用已知思考：`thinking: ...` 配置错误。
- 模型不支持所请求的思考参数时，错误原样透传，不静默降级。

### 相关参考

- [配置第一个 Provider](/coden/zh/docs/start/provider-and-model/)
- [环境变量](/coden/zh/docs/operate/configuration/)
- [语言与思考等级](/coden/zh/docs/agent/compaction-and-thinking/)
- [字段参考](/coden/zh/docs/reference/configuration/)
